import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Careers — Build the Future of Private AI",
  description: "Join TechFides. Open positions in AI engineering, research, consulting, and full-stack development. Remote-first, 3 global offices.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
