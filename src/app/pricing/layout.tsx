import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Transparent Pricing & ROI Calculator",
  description: "No hidden fees. Silver ($500/mo), Gold ($1,000/mo), Platinum ($2,500+/mo). Calculate your 36-month savings vs cloud AI. $0 installation.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
