import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Pricing — Private AI, Monthly Subscription",
  description: "Four tiers from $1,299 to $6,999 per month. Hardware included. Cancel anytime with 30 days' notice.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
