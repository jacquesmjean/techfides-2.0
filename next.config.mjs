/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",  // Uncomment for GitHub Pages build
  images: {
    unoptimized: true,
  },
  // Update this to your GitHub repo name for GitHub Pages
  // basePath: "/techfides-2.0",
  // assetPrefix: "/techfides-2.0/",
};

export default nextConfig;
